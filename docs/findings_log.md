# 발견 로그 (에이전트별 주요 성과 — 실행해 확인한 것만)

| 날짜 | 에이전트 | 발견 | 조치 |
|---|---|---|---|
| 2026-09-08 | 오케스트레이터 | 세션 환경에서 PhysioNet·Kaggle·Zenodo·HuggingFace 차단, AWS S3(OpenNeuro/MSD) 만 접근 가능 | 데이터셋을 S3 직접 다운로드 가능한 SOOP(ds004889) 으로 확정 |
| 2026-09-08 | 오케스트레이터 | SOOP TRACE/ADC 는 4D `(H,W,S,1)`, 마스크는 3D → 형상 비교가 전부 불일치로 나옴 | 인덱스에서 앞 3차원만 비교 (`trace_shape3`) |
| 2026-09-08 | 오케스트레이터 | 급성 마스크 10명은 라벨값 2 또는 3 (`mask_unique_values`) | `>0` 이진화, A2 가 의미 확인 예정 |
| 2026-09-08 | 오케스트레이터 | participants.tsv 나이 `89+` 문자열 → float 파싱 실패 | 89 로 매핑 + `age_clamped_89plus` 플래그 |
| 2026-09-08 | 오케스트레이터 | 영상 1,715명 중 병인 라벨 보유 594명(+Cryptogenic 486), tsv 에 없는 210명 | 분류기는 라벨 보유자만 사용, 분할은 1,451명 전원 |
| 2026-09-08 | A1 | JBS-01K 는 DWI 병변 분할 + 유형 분류로 본 과제와 1:1 대응. 갭 P0: DICOM I/O, 2.5D/3D, 민감도·특이도 보고 | 민감도·특이도·검출 혼동행렬을 `metrics.py` 에 반영, 나머지는 선택 확장으로 헌장에 기록 |
| 2026-09-08 | Astra | (1) 마스크 보유 1,451명 전원 양성 → 환자 단위 특이도 계산 불가, A1 의 "민감도·특이도 P0" 반박 (2) A1 이 인용한 DSC 0.523→0.742 는 판독자 연구 수치이지 모델 단독 Dice 가 아님 (3) SVO 중앙값 1.13 mL vs 타군 10~23 mL → 부피만으로 분리 (4) 급성 마스크+4클래스 라벨 동시 보유 527명, Cryptogenic 403명은 제외해야 함 (5) 단일 기관·Philips 98 % → 기관 홀드아웃 불가 | 특이도 삭제·부피 오차 보고 추가, 분류기 기본값에서 Cryptogenic 제외(`--include-cryptogenic` 로만 포함), 부피 단독 기준선·LAA-vs-CE AUC 보고, DICOM 확장 강등 |
| 2026-09-08 | 스모크 테스트 | 40명/2 epoch CPU: 15 s/epoch, val Dice 0.12→0.22, grad clip(1.0) 발동 0 %, grad-norm p95 0.82 | 파이프라인 동작 확인. 성능 수치 아님 |
| 2026-09-09 | A2 | (1) 인계 수치 전부 재현, 불일치 0건 (2) **부피 특징 1개만으로 4클래스 macro OvR AUC 0.650** → 헌장의 "크기 제외 macro AUC > 0.60" 기준이 GT 부피만으로 이미 초과. 크기 신호는 거의 전부 SVO 발(SVO 제외 시 size-only macro 0.663→0.556) (3) 성분 수(ρ=0.44)·NIHSS(ρ=0.47)도 부피 대리변수라 "크기 제외 세트"가 실제로 크기-무관이 아님 (4) 임상변수 결측 399명은 단일 블록이고 병변이 절반 크기(4.74 vs 8.70 mL)·비-Philips 90 % 포함 → MCAR 아님. GE·Siemens 39명 전원 병인 라벨 없음(분류 코호트 100 % Philips) (5) `acuteischaemicstroke` 는 관측값이 전부 1(0 없음) → 특이도 계산 불가 재확인 | 성공기준을 "부피 단독 기준선(0.650) 대비 증분 + LAA-vs-CE AUC" 로 재정의하도록 A6 에 이관, 크기제외 세트를 스케일 불변 특징으로 재정의, 분류 특징에서 장비·자장·`acuteischaemicstroke` 제외, 결측군 제외로 인한 선택 편향 문장을 README 한계에 고정 |
| 2026-09-09 | A2 | **미해결 항목 종결**: 급성 마스크 값이 1이 아닌 케이스는 정확히 10명(값 2가 8명, 값 3이 2명, subject-ID 연속 블록 503–509·1252–1257). 각 마스크의 비-0 값은 1종류뿐이고(전 코호트에서 다중 값 마스크 0건), TRACE 3.07배·ADC 0.81배로 정상 마스크와 신호 특성 동일, derivatives 에 문서·사이드카 0개 → 하위 클래스가 아니라 강도 코딩 아티팩트 | `>0` 이진화 **[타당]** 확정. 결합 마스크 `desc-lesion_mask` 는 인코딩 비일관(sub-507 = {1,4})이라 사용 금지. 추가로 급성=만성 마스크 완전 동일 3명·급성이 만성에 포함 5명을 A3 에 이관 |
| 2026-09-09 | A2 | 데이터셋 비교표 검증(이번 세션은 웹 접근 가능): ISLES'22 = CC BY 4.0·1.69 GB, ISLES'24 = **CC BY-NC-SA 4.0·99 GB**(Astra 의 표기 불일치 해소), ATLAS 는 **R3.0 n=1,453** 로 갱신(Astra 의 655 는 v2.0 학습분), CT-ICH = CC BY 4.0·n=82, ds004889 최신 스냅샷 = **v1.1.2**(로컬과 동일). CQ500 은 접근 실패로 미확인 | SOOP 유지 근거 확정(유일한 병인 라벨 보유 공개 DWI 셋 · CC0 · 1,451 마스크 · 2.83 GB). 비교표 갱신값을 README/헌장에 반영 |
| 2026-09-09 | A3 | **재현성 확인**: seed 2026 로 split 3회 재실행 → `splits.json`·`splits_strata.csv` 바이트 단위 동일(sha256 `0f923acd…` = 헌장 값), train/val/test 교집합 3쌍 모두 공집합·환자 단위. 전처리 1,233명 전량 재실행 → **1,188명 바이트 단위 동일**(달라진 45명은 전부 아래 버그 수정 효과, **마스크 배열은 1,233명 전원 불변**). **전처리 버그 2건**: (1) `robust_zscore` 가 MAD=0 을 방어하지 않아 **ADC 채널이 ±6 포화** — SOOP ADC 는 TRACE 전경의 절반 이상이 단일 채움값이라 MAD 가 정확히 0, train+val **44/1233명(3.6 %)** 해당, 최악 sub-683 은 복셀 64.4 %가 클립값 (2) **ADC↔TRACE affine 미검사** — sub-235 의 ADC 격자가 TRACE 대비 중심 6.57 mm·모서리 15.5~27.3 mm 어긋난 채 2채널로 스택됨(sub-1233 은 canonical 변환이 정상 흡수한 오탐). (3) `SliceDataset` 증강 RNG 가 DataLoader 워커로 fork 되어 `num_workers>0` 시 재현 불가(현 config 는 0 이라 잠복). **미수정 발견**: 클립 ±6 이 좁아 **병변 복셀의 평균 34.6 %가 TRACE 채널에서 포화**(397명은 병변의 50 % 이상) → 전체 캐시 무효화 사안이라 A4 실험으로 이관. **누수 검증**: 정규화는 환자별 자기 통계만 쓰는 구조라 누수 불가, `data/cache` 에 test 환자 0명, test 조기 열람 경로 없음(단 층화가 test 병변 크기 구간을 사용한 점은 기록) | `preprocess.py` 에 `robust_stats()` MAD=0 폴백 + ADC 를 TRACE 격자로 리샘플, `dataset.py` 에 항목별 결정론적 증강 RNG, `split.py` FutureWarning 제거(해시 불변), `metrics.VolumeAgreement` 에 `mape_pct`·`median_abs_pct_err` 추가(A2 항목 5). 회귀 테스트 20개 추가(`test_preprocess_geometry.py`·`test_no_test_leakage.py`·`test_dataset_rng.py`) → **pytest 15 → 35 통과**. 분석 스크립트 `scripts/analysis/a3_{checks,stats}.py`(기본 `--which train val`), 산출물 `results/a3/`. **A4 는 학습 전에 `results/a3/stale_cache_subjects.txt` 의 45명분 캐시를 재생성할 것**(약 2초) |
| 2026-09-09 | A3 | A2 이관 6건 종결: (1) `(mk>0)` 유지 — 값≠1 인 환자는 train+val 에 9명(값 2 = sub-503/504/505/507/508/1252/1253, 값 3 = sub-1255/1257, 나머지 1명은 test), 근거를 `preprocess.py` docstring 에 명문화 (2) 결합 마스크 `desc-lesion_mask` 는 **복셀을 읽는 코드가 저장소에 없음** 확인 (3) 급성=만성 완전 동일 **3명**(sub-235/1720/1737), 급성⊂만성 **5명**(+sub-1095/1132) — 만성 마스크 보유 168명 전수 비교(test 는 규칙상 미측정) (4) 128² 안전성: 빈 마스크 0건, 부피 보존비 p50 **0.990**(p1 0.836/p99 1.132), 최소 병변 **3복셀** (5) native 복셀 부피 1.449~8.610 mm³(5.9배)·병변 0.067~557 mL → 부피 오차 mL+% 병기 필수 (6) `acuteischaemicstroke` 는 이미 특징에서 제외됨. **기술통계(train/val)**: 슬라이스 25,963/5,593, 양성 슬라이스 비율 0.2888/0.2900, 병변 중앙값 8.20/9.82 mL, 소병변(<2 mL) 28.0/27.5 %, 병변 TRACE z 중앙값 +5.63(정상 −1.12)·ADC z 병변 −0.19(대비 약함), ADC 원 강도 환자 간 p95/p5 **69배** | 마스크 이진성 1,233/1,233 확인, 정렬 대조검정(병변 vs 좌우반전 병변) **1,227/1,233 통과**. 급성/만성 플래그 `results/a3/acute_chronic_flags.csv` 를 A6 로, ADC 채널 ablation·클립 실험을 A4 로 이관. test 는 **218명이라는 개수만** 기록하고 어떤 통계도 내지 않음 |
| 2026-09-09 | A4 | **정규화 클립 ±6 이 성능을 크게 깎고 있었다(A3 F3 이관 항목 확정)**: 원본 격자에서 재측정한 결과 급성 병변 복셀의 TRACE z 중앙값이 **+6.24**, 병변 복셀의 **50.7 %가 z > 6** 인 반면 정상 뇌는 1.9 % 뿐(train 150명 표본). 동일 config·동일 시드 24 epoch 학습 비교에서 **clip 6 → val Dice 0.5950 / clip 10 → 0.6826 / clip 14 → 0.6799 / 환자별 백분위 → 0.6804**, 부피 ICC 는 0.912 → 0.959. 시드 잡음 폭(±0.007, 3시드 실측)의 **12배** 차이 | `configs/seg_unet2d.yaml` 에 `clip: 10.0`·`clip_mode: mad` 확정, `robust_zscore`/`preprocess_subject`/`scripts/preprocess.py` 에 클립 파라미터 도입, **train+val 1,233명 캐시 전량 재생성 완료**(오류 0, 약 20초). 마스크·`voxel_volume_mm3` 불변 확인, 이전 캐시는 `data/cache_clip06/` 에 보존 |
| 2026-09-09 | A4 | **학습 설정 확정 + 인프라 버그**: (1) `configs` 의 `size` 키를 **어떤 코드도 읽지 않아** yaml 을 192 로 고쳐도 128² 캐시로 조용히 학습되는 구조였다 (2) GPU 경로 전무(AMP·`channels_last`·`pin_memory`·`num_workers` 없음) — bf16 + `channels_last` 로 step 처리량 1,580 → 3,695 slices/s, epoch 11.18 s → 5.71 s. 속도 이득의 대부분은 `channels_last`(+83 %)이고 `cudnn.benchmark` 는 **+2.1 % 뿐**이라 **재현성을 깨지 않았다** (3) `num_workers` 0/8/16 과 동일 시드 재실행이 **train_loss·grad-norm·val Dice 소수점 15자리까지 일치**(헌장 <0.02 충족) (4) 런북의 "grad clip 발동 0 %"는 40명 CPU 스모크 산물 — 실제로는 epoch 2~4 에 **20~30 %** 발동 (5) epoch 예산 실측: 24/40/60/100/150 → Dice 0.6826/0.6847/0.6944/0.6947/0.6944 지만 부피 ICC 는 0.956/0.914/0.866/0.857/0.828 로 **단조 감소** (6) ADC ablation: 2ch 0.6826 vs TRACE-only 0.6803(Dice 기여 없음) vs ADC-only 0.4449, 단 부피 ICC 는 0.959 vs 0.813 (7) `size` 192 는 0.6812 로 이득 없이 epoch 1.9배 (8) lr 3e-3/3e-4, `base_channels` 32, `dice_reduction: sample`, `pos_weight` 8, `neg_pos_ratio` 2 모두 잡음 이내 또는 열세 | **확정 config**: `epochs 40, batch_size 64, num_workers 8, amp bf16, channels_last true, pin_memory true, cudnn_benchmark false, size 128, clip 10.0, lr 1e-3, grad_clip 1.0, base 16/depth 4`. **전체 학습 예상 4분 40초**(40 epoch 완주 실측 279 s). `train.py` 에 AMP/channels_last/채널선택/캐시해상도 assert 추가, `preprocess.py` 가 config 에서 `size`/`clip` 을 읽도록 단일화(+`run_all.sh`), `soft_dice_loss` 에 `reduction` 스위치, 회귀 테스트 7개 추가 → **pytest 35 → 42 통과**. Dice–ICC 트레이드오프(40 vs 60 epoch)·ADC 의 ICC 기여·ICC 시드변동(±0.05)·`clip` 불일치 미검출을 **A6 로 이관** |
| (서버) | A5a·A5b·A6 | | |
