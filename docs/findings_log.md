# 발견 로그 (에이전트별 주요 성과 — 실행해 확인한 것만)

| 날짜 | 에이전트 | 발견 | 조치 |
|---|---|---|---|
| 2026-09-08 | 오케스트레이터 | 세션 환경에서 PhysioNet·Kaggle·Zenodo·HuggingFace 차단, AWS S3(OpenNeuro/MSD) 만 접근 가능 | 데이터셋을 S3 직접 다운로드 가능한 SOOP(ds004889) 으로 확정 |
| 2026-09-08 | 오케스트레이터 | SOOP TRACE/ADC 는 4D `(H,W,S,1)`, 마스크는 3D → 형상 비교가 전부 불일치로 나옴 | 인덱스에서 앞 3차원만 비교 (`trace_shape3`) |
| 2026-09-08 | 오케스트레이터 | 급성 마스크 10명은 라벨값 2 또는 3 (`mask_unique_values`) | `>0` 이진화, A2 가 의미 확인 예정 |
| 2026-09-08 | 오케스트레이터 | participants.tsv 나이 `89+` 문자열 → float 파싱 실패 | 89 로 매핑 + `age_clamped_89plus` 플래그 |
| 2026-09-08 | 오케스트레이터 | 영상 1,715명 중 병인 라벨 보유 594명(+Cryptogenic 486), tsv 에 없는 210명 | 분류기는 라벨 보유자만 사용, 분할은 1,451명 전원 |
| 2026-09-08 | A1 | JBS-01K 는 DWI 병변 분할 + 유형 분류로 본 과제와 1:1 대응. 갭 P0: DICOM I/O, 2.5D/3D, 민감도·특이도 보고 | 민감도·특이도·검출 혼동행렬을 `metrics.py` 에 반영, 나머지는 선택 확장으로 헌장에 기록 |
| 2026-09-08 | Astra | (1) 마스크 보유 1,451명 전원 양성 → 환자 단위 특이도 계산 불가, A1 의 "민감도·특이도 P0" 반박 (2) A1 이 인용한 DSC 0.523→0.742 는 판독자 연구 수치이지 모델 단독 Dice 가 아님 (3) SVO 중앙값 1.13 mL vs 타군 10~23 mL → 부피만으로 분리 (4) 급성 마스크+4클래스 라벨 동시 보유 527명, Cryptogenic 403명은 제외해야 함 (5) 단일 기관·Philips 98 % → 기관 홀드아웃 불가 | 특이도 삭제·부피 오차 보고 추가, 분류기 기본값에서 Cryptogenic 제외(`--include-cryptogenic` 로만 포함), 부피 단독 기준선·LAA-vs-CE AUC 보고, DICOM 확장 강등 |
| 2026-09-08 | 스모크 테스트 | 40명/2 epoch CPU: 15 s/epoch, val Dice 0.12→0.22, grad clip(1.0) 발동 0 %, grad-norm p95 0.82 | 파이프라인 동작 확인. 성능 수치 아님 |
| 2026-09-09 | A2 | (1) 인계 수치 전부 재현, 불일치 0건 (2) **부피 특징 1개만으로 4클래스 macro OvR AUC 0.650** → 헌장의 "크기 제외 macro AUC > 0.60" 기준이 GT 부피만으로 이미 초과. 크기 신호는 거의 전부 SVO 발(SVO 제외 시 size-only macro 0.663→0.556) (3) 성분 수(ρ=0.44)·NIHSS(ρ=0.47)도 부피 대리변수라 "크기 제외 세트"가 실제로 크기-무관이 아님 (4) 임상변수 결측 399명은 단일 블록이고 병변이 절반 크기(4.74 vs 8.70 mL)·비-Philips 90 % 포함 → MCAR 아님. GE·Siemens 39명 전원 병인 라벨 없음(분류 코호트 100 % Philips) (5) `acuteischaemicstroke` 는 관측값이 전부 1(0 없음) → 특이도 계산 불가 재확인 | 성공기준을 "부피 단독 기준선(0.650) 대비 증분 + LAA-vs-CE AUC" 로 재정의하도록 A6 에 이관, 크기제외 세트를 스케일 불변 특징으로 재정의, 분류 특징에서 장비·자장·`acuteischaemicstroke` 제외, 결측군 제외로 인한 선택 편향 문장을 README 한계에 고정 |
| 2026-09-09 | A2 | **미해결 항목 종결**: 급성 마스크 값이 1이 아닌 케이스는 정확히 10명(값 2가 8명, 값 3이 2명, subject-ID 연속 블록 503–509·1252–1257). 각 마스크의 비-0 값은 1종류뿐이고(전 코호트에서 다중 값 마스크 0건), TRACE 3.07배·ADC 0.81배로 정상 마스크와 신호 특성 동일, derivatives 에 문서·사이드카 0개 → 하위 클래스가 아니라 강도 코딩 아티팩트 | `>0` 이진화 **[타당]** 확정. 결합 마스크 `desc-lesion_mask` 는 인코딩 비일관(sub-507 = {1,4})이라 사용 금지. 추가로 급성=만성 마스크 완전 동일 3명·급성이 만성에 포함 5명을 A3 에 이관 |
| 2026-09-09 | A2 | 데이터셋 비교표 검증(이번 세션은 웹 접근 가능): ISLES'22 = CC BY 4.0·1.69 GB, ISLES'24 = **CC BY-NC-SA 4.0·99 GB**(Astra 의 표기 불일치 해소), ATLAS 는 **R3.0 n=1,453** 로 갱신(Astra 의 655 는 v2.0 학습분), CT-ICH = CC BY 4.0·n=82, ds004889 최신 스냅샷 = **v1.1.2**(로컬과 동일). CQ500 은 접근 실패로 미확인 | SOOP 유지 근거 확정(유일한 병인 라벨 보유 공개 DWI 셋 · CC0 · 1,451 마스크 · 2.83 GB). 비교표 갱신값을 README/헌장에 반영 |
| 2026-09-09 | A3 | **재현성 확인**: seed 2026 로 split 3회 재실행 → `splits.json`·`splits_strata.csv` 바이트 단위 동일(sha256 `0f923acd…` = 헌장 값), train/val/test 교집합 3쌍 모두 공집합·환자 단위. 전처리 1,233명 전량 재실행 → **1,188명 바이트 단위 동일**(달라진 45명은 전부 아래 버그 수정 효과, **마스크 배열은 1,233명 전원 불변**). **전처리 버그 2건**: (1) `robust_zscore` 가 MAD=0 을 방어하지 않아 **ADC 채널이 ±6 포화** — SOOP ADC 는 TRACE 전경의 절반 이상이 단일 채움값이라 MAD 가 정확히 0, train+val **44/1233명(3.6 %)** 해당, 최악 sub-683 은 복셀 64.4 %가 클립값 (2) **ADC↔TRACE affine 미검사** — sub-235 의 ADC 격자가 TRACE 대비 중심 6.57 mm·모서리 15.5~27.3 mm 어긋난 채 2채널로 스택됨(sub-1233 은 canonical 변환이 정상 흡수한 오탐). (3) `SliceDataset` 증강 RNG 가 DataLoader 워커로 fork 되어 `num_workers>0` 시 재현 불가(현 config 는 0 이라 잠복). **미수정 발견**: 클립 ±6 이 좁아 **병변 복셀의 평균 34.6 %가 TRACE 채널에서 포화**(397명은 병변의 50 % 이상) → 전체 캐시 무효화 사안이라 A4 실험으로 이관. **누수 검증**: 정규화는 환자별 자기 통계만 쓰는 구조라 누수 불가, `data/cache` 에 test 환자 0명, test 조기 열람 경로 없음(단 층화가 test 병변 크기 구간을 사용한 점은 기록) | `preprocess.py` 에 `robust_stats()` MAD=0 폴백 + ADC 를 TRACE 격자로 리샘플, `dataset.py` 에 항목별 결정론적 증강 RNG, `split.py` FutureWarning 제거(해시 불변), `metrics.VolumeAgreement` 에 `mape_pct`·`median_abs_pct_err` 추가(A2 항목 5). 회귀 테스트 20개 추가(`test_preprocess_geometry.py`·`test_no_test_leakage.py`·`test_dataset_rng.py`) → **pytest 15 → 35 통과**. 분석 스크립트 `scripts/analysis/a3_{checks,stats}.py`(기본 `--which train val`), 산출물 `results/a3/`. **A4 는 학습 전에 `results/a3/stale_cache_subjects.txt` 의 45명분 캐시를 재생성할 것**(약 2초) |
| 2026-09-09 | A3 | A2 이관 6건 종결: (1) `(mk>0)` 유지 — 값≠1 인 환자는 train+val 에 9명(값 2 = sub-503/504/505/507/508/1252/1253, 값 3 = sub-1255/1257, 나머지 1명은 test), 근거를 `preprocess.py` docstring 에 명문화 (2) 결합 마스크 `desc-lesion_mask` 는 **복셀을 읽는 코드가 저장소에 없음** 확인 (3) 급성=만성 완전 동일 **3명**(sub-235/1720/1737), 급성⊂만성 **5명**(+sub-1095/1132) — 만성 마스크 보유 168명 전수 비교(test 는 규칙상 미측정) (4) 128² 안전성: 빈 마스크 0건, 부피 보존비 p50 **0.990**(p1 0.836/p99 1.132), 최소 병변 **3복셀** (5) native 복셀 부피 1.449~8.610 mm³(5.9배)·병변 0.067~557 mL → 부피 오차 mL+% 병기 필수 (6) `acuteischaemicstroke` 는 이미 특징에서 제외됨. **기술통계(train/val)**: 슬라이스 25,963/5,593, 양성 슬라이스 비율 0.2888/0.2900, 병변 중앙값 8.20/9.82 mL, 소병변(<2 mL) 28.0/27.5 %, 병변 TRACE z 중앙값 +5.63(정상 −1.12)·ADC z 병변 −0.19(대비 약함), ADC 원 강도 환자 간 p95/p5 **69배** | 마스크 이진성 1,233/1,233 확인, 정렬 대조검정(병변 vs 좌우반전 병변) **1,227/1,233 통과**. 급성/만성 플래그 `results/a3/acute_chronic_flags.csv` 를 A6 로, ADC 채널 ablation·클립 실험을 A4 로 이관. test 는 **218명이라는 개수만** 기록하고 어떤 통계도 내지 않음 |
| 2026-09-09 | A4 | **정규화 클립 ±6 이 성능을 크게 깎고 있었다(A3 F3 이관 항목 확정)**: 원본 격자에서 재측정한 결과 급성 병변 복셀의 TRACE z 중앙값이 **+6.24**, 병변 복셀의 **50.7 %가 z > 6** 인 반면 정상 뇌는 1.9 % 뿐(train 150명 표본). 동일 config·동일 시드 24 epoch 학습 비교에서 **clip 6 → val Dice 0.5950 / clip 10 → 0.6826 / clip 14 → 0.6799 / 환자별 백분위 → 0.6804**, 부피 ICC 는 0.912 → 0.959. 시드 잡음 폭(±0.007, 3시드 실측)의 **12배** 차이 | `configs/seg_unet2d.yaml` 에 `clip: 10.0`·`clip_mode: mad` 확정, `robust_zscore`/`preprocess_subject`/`scripts/preprocess.py` 에 클립 파라미터 도입, **train+val 1,233명 캐시 전량 재생성 완료**(오류 0, 약 20초). 마스크·`voxel_volume_mm3` 불변 확인, 이전 캐시는 `data/cache_clip06/` 에 보존 |
| 2026-09-09 | A4 | **학습 설정 확정 + 인프라 버그**: (1) `configs` 의 `size` 키를 **어떤 코드도 읽지 않아** yaml 을 192 로 고쳐도 128² 캐시로 조용히 학습되는 구조였다 (2) GPU 경로 전무(AMP·`channels_last`·`pin_memory`·`num_workers` 없음) — bf16 + `channels_last` 로 step 처리량 1,580 → 3,695 slices/s, epoch 11.18 s → 5.71 s. 속도 이득의 대부분은 `channels_last`(+83 %)이고 `cudnn.benchmark` 는 **+2.1 % 뿐**이라 **재현성을 깨지 않았다** (3) `num_workers` 0/8/16 과 동일 시드 재실행이 **train_loss·grad-norm·val Dice 소수점 15자리까지 일치**(헌장 <0.02 충족) (4) 런북의 "grad clip 발동 0 %"는 40명 CPU 스모크 산물 — 실제로는 epoch 2~4 에 **20~30 %** 발동 (5) epoch 예산 실측: 24/40/60/100/150 → Dice 0.6826/0.6847/0.6944/0.6947/0.6944 지만 부피 ICC 는 0.956/0.914/0.866/0.857/0.828 로 **단조 감소** (6) ADC ablation: 2ch 0.6826 vs TRACE-only 0.6803(Dice 기여 없음) vs ADC-only 0.4449, 단 부피 ICC 는 0.959 vs 0.813 (7) `size` 192 는 0.6812 로 이득 없이 epoch 1.9배 (8) lr 3e-3/3e-4, `base_channels` 32, `dice_reduction: sample`, `pos_weight` 8, `neg_pos_ratio` 2 모두 잡음 이내 또는 열세 | **확정 config**: `epochs 40, batch_size 64, num_workers 8, amp bf16, channels_last true, pin_memory true, cudnn_benchmark false, size 128, clip 10.0, lr 1e-3, grad_clip 1.0, base 16/depth 4`. **전체 학습 예상 4분 40초**(40 epoch 완주 실측 279 s). `train.py` 에 AMP/channels_last/채널선택/캐시해상도 assert 추가, `preprocess.py` 가 config 에서 `size`/`clip` 을 읽도록 단일화(+`run_all.sh`), `soft_dice_loss` 에 `reduction` 스위치, 회귀 테스트 7개 추가 → **pytest 35 → 42 통과**. Dice–ICC 트레이드오프(40 vs 60 epoch)·ADC 의 ICC 기여·ICC 시드변동(±0.05)·`clip` 불일치 미검출을 **A6 로 이관** |
| 2026-09-09 | A6 | **헌장의 병인 성공기준이 무의미함을 자체 파이프라인에서 확정하고 대체 기준을 test 열람 전에 확정**: (1) `volume_only`(특징 1개 = log 부피) 세트를 만들어 측정 → 4클래스 macro OvR AUC **0.6421**(A2 의 0.650 재현) → "크기 제외 macro AUC > 0.60" 은 **학습 없이 통과**하는 기준 (2) `no_size` 가 `full` 보다 높고(0.7376 vs 0.7301, GT) 그 안의 `n_components` ρ=+0.476·`NIHSS` ρ=+0.471 → **"크기 제외"가 크기 제외가 아님** (3) **LAA-vs-CE AUC 를 신규 구현**(헌장이 요구했으나 코드에 없었음) → 예측 마스크 `full` **0.6551 [0.595, 0.714]**, GT 0.6409 (4) **SVO 제외 3클래스**에서 부피 단독은 **0.5125 [0.467, 0.557] = 우연**인데 `full` 은 **0.6551 [0.603, 0.702]**, Δ +0.1426 [+0.088, +0.194] → 모델의 실질 신호는 크기가 아님 (5) 짝지은 부트스트랩 Δmacro(`full`−`volume_only`) = **+0.0746 [+0.038, +0.111]**, p(Δ>0)=1.000 (6) 프로토콜 누수 없음: 자장세기 단독 병인 macro AUC 0.4919, 복셀기하 0.4561(A2 이관 4b) | 헌장 성공기준을 **v2** 로 개정: "macro AUC(크기제외) > 0.60" **삭제** → **E1 LAA-vs-CE > 0.55**(PASS/판정불가/FAIL 3분류) · **E2 부피 단독 대비 Δmacro > 0** · **E3 SVO 제외 3클래스 > 0.55** 로 대체, v1 원문·변경사유·검정력을 헌장에 병기. "크기 제외 세트"를 **`scale_invariant`**(무차원 ∧ dev \|ρ(log부피)\| < 0.25 → largest_component_frac·centroid_{x,y,z}·age·sex)로 재정의. `metrics.laa_vs_ce_auc` 신설(CLAUDE.md 지표 단일화 규칙), `train_cls.py` 에 `volume_only`·`scale_invariant`·SVO제외 3클래스·짝지은 부트스트랩 CI 추가 |
| 2026-09-09 | A6 | **학습 진단·threshold 판정**: (1) 누수 신호 없음 — val Dice epoch0 **0.3370**(best 의 49 %), best 의 90 % 도달까지 5 epoch, train_loss–val_dice Spearman **−0.926**(완전 −1 아님), 값 동일 epoch 0건 (2) lr 이 `cosine_warmup` 공식과 **최대 오차 0.0**, 피크 이후 단조 감소, warmup 468/9360 step (3) grad clip 발동 **epoch 2 에 22.2 %**, 40 epoch 평균 3.2 %, p95 최대 1.340 → **런북 §5 의 "0 %" 문장 폐기 처리**(A4 이관 7) (4) **threshold 를 val 로 고를 이유가 없음**: 0.1~0.9 전 구간 Dice 폭 **0.0057** = 부트스트랩 SE(0.019)의 0.30배, val 최적 0.55 의 이득 +0.0004 → **0.5 고정**. min-voxels 후처리는 전 구간 해로움(20복셀에서 민감도 0.972→0.908) (5) **부피 ICC 95 % CI [0.794, 0.990] 이 헌장 임계값 0.85 를 포함** → 점추정 단독 보고 금지 (6) val 선택 과적합 무시 가능: best−마지막5평균 Dice +0.0037(0.19 SE)·ICC +0.0118 (7) 재현 run 과 val Dice **차이 0.000** (8) 추론 정밀도 차이 발견: 학습로그(bf16) 0.68474 vs `eval_seg.py`(fp32) **0.68550** | `metrics.py` 에 `bootstrap_icc_ci()`·구간별 `detect_frac`/`median_abs_pct_err`/`dice_median`/`median_gt_ml` 추가, `diagnose.py` 전면 보완(**캐시 매니페스트 vs run config 대조** = A4 이관 4, 라벨품질 플래그 교차, Bland-Altman 플롯, 검출실패 목록, 층화 caveat 기록). 분석 스크립트 `scripts/analysis/a6_{curves,threshold,failures,confound,power}.py` 신설. 회귀 테스트 +4 → **pytest 42 → 46 통과**. 보고 기준 val 값을 test 와 같은 fp32 경로(`eval_val.json`)로 통일 |
| 2026-09-09 | A6 | **실패 사례의 절반은 모델이 아니라 라벨**: 병변 내부 vs 주변 고리(4복셀 팽창)의 z 대비를 재보니 val 218명 중 **11명(5.0 %)이 병변 내부 ADC 가 주변보다 높다**(급성 경색이면 낮아야 함 = 만성 병변 서명). 이 11명 평균 Dice **0.172** vs 나머지 **0.713** → val Dice 0.6855 중 약 **0.027 이 입력에 증거가 없는 라벨 때문**. 검출 완전 실패 6명 중 5명은 <3 mL 소병변, 유일한 대형 실패 `sub-127`(107 mL, Siemens)도 ADC 서명이 반대. Dice 와 TRACE 대비의 Spearman **+0.469**, log 부피와는 +0.543. A3 가 플래그한 급성=만성 8명은 **전원 train** 이라 val 지표 영향 0명 | 소병변 구간 보고를 **Dice + 검출률 + 중앙값 %오차**로 의무화(<2 mL: Dice 0.541 / 검출 0.950 / %오차 43.0 %). test 에서도 동일 분석을 사전 등록(전체 지표로만 합격 판정, ADC 서명 반대군 제외는 민감도 분석으로만 보고) |
| 2026-09-09 | A6 | **테스트셋 1회 평가 승인(사인오프)**. 고정 설정: `runs/seg_unet2d/best.pt`(epoch 33) · threshold **0.5** · min-voxels 0 · TTA 없음 · fp32 · size 128/clip 10.0 캐시 · splits sha256 `0f923acd…` · logreg C=0.5 seed 2026. **재학습 불필요** — 60 epoch 은 Dice +0.010 대신 ICC 0.914→0.866(CI 하한이 이미 0.794), `dice_reduction: sample` 은 0.6772 < 0.6826, 라벨품질 8명은 train 의 0.5 %. **검정력 사전 계산**: test 병인 코호트 ≈ 79명(추정, LAA 35/CE 22/SVO 13/Others 9) → LAA-vs-CE 의 Hanley–McNeil SE 0.073 → 임계 0.55 에서 검정력 0.93·오통과 0.26(0.60 이면 0.78/0.10) → **E1 임계값 0.55 채택**. S1 여유 7.1 SE, S2 여유 6.5 SE | 사전 등록 기대치와 실행 명령을 `docs/agents/A6_report.md` §9 와 `scripts/run_all.sh` 말미(here-doc, 실행되지 않음)에 고정. **test 결과가 기준 미달이어도 그대로 보고**하고 기준을 사후 수정하지 않는다고 선언. 홀드아웃 caveat 확장 확인: `split.stratum()` 이 `etiology_code` **와** `mask_acute_ml` 을 쓰므로 test **라벨 메타데이터**가 split 경계에 반영됨(A3 F9 확장) → 회귀 테스트로 고정 |
| 2026-09-09 | A5b | **A5a 17건 판정: 동의 13 · 부분동의 4 · 반박 0** (`docs/agents/A5b_report.md`, 실행 모델 Fable). 독립 검증: ICC(2,1)·Dice·LoA·부트스트랩이 독립 구현과 1e-15 이내 일치, split 해시 재현. **확정 1 — 캐시 좌우축**: 캐시 `(S,H,W)` 의 **H 가 좌우**(합성 볼륨 관통 실험 + train/val 1,233명 GT 마스크의 H 저측 분율이 0/1 에 79.7 % 집중 vs W 50.4 %). `features.py` 가 W 를 좌우로 읽어 `laterality_left_frac`/`bilateral` 이 전후 분율이었음 → 수정 후 dev 예측 `full` LAA-vs-CE **0.6551 → 0.6709**, macro 불변. 증강 "left-right flip" 은 실제 **전후 반전** → `flip_axis` config 화(`ap` 로 재학습 시 `best.pt` **바이트 동일** 확인). `lr` 40 epoch × 3 시드: Dice **+0.009**(전 시드 양수), ICC 차이 없음, 7 run 의 ICC 범위 **0.824~0.914**(현 best.pt = 최대값) → **재학습 불필요** 판정(이득이 잡음 이내, val 선택 추가 회피). **확정 2 — test 열람 문구**: `build_index.py` 는 층화 split 에 필수라 전 코호트 마스크 복셀을 읽고(부트스트랩), A2 `a2_volumes.py` 가 split 후 1,451명 마스크 부피를 재계산 → "어떤 스크립트도 읽지 않았다"는 거짓; 학습·선택·평가 투입 흔적은 0. **확정 3 — E1 검정력**: PASS 규칙(CI 하한>0.5 포함)의 실제 통과율은 참값 0.655 에서 **0.54**(판정불가 0.40), 우연일 때 0.017(시뮬레이션 300×2,000). **확정 4 — 검출 정의**: val 9/218 이 Dice 0 인데 TP → 병변 접촉률 0.9312 병기. **부분동의**: v2 는 "dev 결과 본 뒤·test 전 개정"으로 표기(1), 예측 마스크 dev CV 는 분류 단계 CV 이나 seg-train/val 부분 AUC 0.717 vs 0.720 으로 낙관 편향 미검출(4), 선택 편향은 미측정(7). 라벨 플래그 합집합 5명(8명 아님) | `features.py`(H=좌우)·`dataset.py`/`train.py`(`flip_axis`)·`metrics.py`(shape 검사, 민감도 NaN, `detection_sensitivity_overlap`)·`train_cls.py`(클래스별·3클래스 CI, fold AUC, seg-split AUC, OOF sidecar, 클래스 결측 재표본 규칙)·`a2_volumes.py`(기본 train+val)·`a6_threshold.py`(fp32)·`preprocess.py` 주석. 회귀 테스트 +6 → **pytest 46 → 52**. CLAUDE.md test 규칙 재정의, 헌장 v2 문구·E1/E3/E4/E5·한계 5·6·§C 갱신, README 정정. **테스트셋 평가: 조건부 승인** — 조건은 A5b 보고서 §6 |
| 2026-09-09 | A5a | **외부 심사(Codex CLI / OpenAI GPT-6, `gpt-6-astra`) — 독립 판정 [보류]**. `metrics.py` 전 함수를 NumPy 로 독립 구현해 `rtol=atol=1e-10` 일치 확인(ICC(2,1) 은 Shrout & Fleiss 정의와 대조), val 218명을 저장 마스크에서 재계산해 `eval_val.json` 과 전 필드 일치 → **지표 계층은 결함 없음**. 그러나 방법론 4건이 [높음]: (1) 헌장 v2 는 **dev 결과를 본 뒤의 개정**이라 "결과를 보기 전 확정"으로 제시하면 부정확(test 를 보고 바꾼 증거는 없음) (2) **"어떤 스크립트도 test 를 읽지 않았다"는 문자 그대로 거짓** — `index.py` 가 층화 split 을 위해 전 코호트 마스크 복셀을 읽고, A2 `a2_volumes.py` 가 split 확정 후 test 218명 포함 1,451명 마스크를 재계산(02:55 KST). 단 test 모델 평가·예측·학습 투입 흔적은 0, 캐시 test 0명 (3) **RAS 좌우축이 특징·증강에서 뒤바뀜** — 캐시가 `(Z,X,Y)` 라 H 가 좌우인데 `features.py` 는 W 를 좌우로 읽음. 환자 좌측 병변 합성 볼륨에서 `laterality_left_frac` 이 1.0 이 아닌 **0.5** (4) 예측 마스크 dev CV 는 전체 파이프라인 OOF 가 아님(447명 중 **367명이 분할 모델 train**, 80명이 val) | 저장소 코드는 수정하지 않고 보고서만 작성(역할 규칙). 17건 전부에 번호·심각도·[확인됨/추정]·재현 코드를 붙여 A5b 판정 대상으로 넘김 |
| 2026-09-09 | 오케스트레이터 | **재현성 실측**: 물리적으로 다른 GPU 2장에서 동일 seed·동일 config 로 40 epoch 전체 학습을 2회 실행 → best epoch 33 동일, val Dice **0.6847418811650405 로 16자리까지 동일**(헌장 S6 기준 <0.02 대비 차이 **0.0000**), `best.pt` **sha256 바이트 동일**(`e10d9268…`). 파이프라인 1~3단계도 클라우드 세션 커밋본과 바이트 동일 재생성(`index.csv`·`splits.json`·`splits_strata.csv`, splits sha256 `0f923acd…`) | 헌장 S6 충족을 실측으로 확정. `runs/seg_unet2d_repro` 보존 |
| 2026-09-09 | 오케스트레이터 | **테스트셋 1회 평가 실행(A6 승인 + A5b 조건부 승인 후, 헌장 §C 고정 절차 그대로)**. 분할 218명: **S1 Dice 0.6877 [0.654, 0.721] ≥0.55 PASS · S1b \|test−val\| 0.0022 ≤0.06 PASS · S2 검출(비어있지 않음) 0.9771 ≥0.90 PASS**(병변 접촉률 0.9679) **· S3 부피 ICC 0.9839 [0.963, 0.992] ≥0.85 PASS** · S4 MAE 5.21 mL·MAPE 37.5 %·중앙값 \|%오차\| 19.8 %·bias −0.74 mL·LoA [−22.0, +20.5] · S5 <2 mL Dice 0.5517/검출 0.950/%오차 42.6 %. **S3 는 A5b 가 "실패 가능성 실질적"이라고 사전 등록한 항목**(val ICC 0.9138 이 7 run 중 최대값, 시드 범위 0.824~0.914)인데 test 가 val 전 범위보다 높게 나옴. 병인 80명(LAA 35/CE 22/SVO 13/Others 10, 예측 마스크): **E2 Δmacro = 0.6687−0.6335 = +0.0352 > 0 PASS · E3 SVO 제외 3클래스 0.6170 [0.506, 0.726] > 0.55 PASS · E1 LAA-vs-CE 0.5909 [0.4376, 0.7389] → 점추정은 0.55 를 넘었으나 CI 가 0.50 을 포함 → 헌장 정의상 [판정 불가]**. **정직한 음성 결과: test 에서 `volume_only` 가 `full` 보다 LAA-vs-CE 가 높다(0.6013 vs 0.5909)** — dev 의 +0.084 우위(0.6709 vs 0.5869)가 역전됐다. GT 마스크 오라클도 같은 역전(0.6234 vs 0.6351)이라 분할 품질 탓이 아니다. 즉 **크기 지름길이 가장 약한 축에서는 모델이 단일 특징 기준선을 넘지 못했다** | 헌장 §C 대로 **재학습·재선택·기준 수정 없음**. E1 을 성공으로 주장하지 않고 [판정 불가] 로 보고. 산출물: `runs/seg_unet2d/eval_test.json`, `results/cls_{pred,gt}_logreg_final.json`(+`.oof.json`), `results/diag_seg_unet2d_test/`, `results/a3_test/` |
| 2026-09-09 | A4b | **소병변(<2 mL) 구간은 개선할 수 있고, 원인은 3D 문맥이 아니라 슬라이스 샘플러의 크기 편향이었다.** (1) **진단 먼저**: <2 mL 60명의 구간 Dice 0.543 은 분할 품질이 아니라 **검출 실패의 산술** — Dice=0 이 시드당 8~10명이고 이들을 빼면 구간 Dice 가 **0.62~0.65 로 [2,10) mL 구간(0.651)과 동일**하다. 찾아낸 소병변의 예측/GT 부피 중앙값은 **1.00~1.19 로 과소분할이 아니다** → **"소병변은 과소분할"이라는 통념은 이 데이터에서 거짓**. 5시드 전부 Dice=0 인 **하드 플로어 7명**은 TRACE 대비 중앙값 **1.63 z**(나머지 2.57 z)이고 A6 의 ADC 서명 반대 플래그 2명이 전부 여기 포함 → 입력 증거 부족. 구간 평균의 시드 SD **0.0138**(환자 1명이 0↔중앙값으로 뒤집히면 0.0115 이동) — 기존 run 들의 0.541~0.579 산포는 실력차가 아니라 n=60 의 이산성. (2) **학습 비중 실측**: <2 mL 환자는 train 의 **28.0 %인데 양성 슬라이스의 10.2 %만** 차지, ≥50 mL 은 18.4 %인데 33.3 % (환자당 2.70 vs 13.37 슬라이스). (3) **판정(40 epoch·동일 시드·사전 등록 임계 = 시드 SD 2배 = +0.0276)**: **H1 해상도 −0.008 [기각] · H3 `dice_reduction: sample` −0.006 [기각] · H4 2.5D 인접슬라이스 4채널 +0.004 / 6채널 −0.004 [기각] · H5 Tversky(FN 가중) −0.028·focal −0.022 [기각, 오히려 악화] · H6 추론 임계값 스윕 [기각] · H2 소병변 오버샘플링 [채택]**. (4) **Tversky 가 왜 해로운지도 같은 분해가 설명**: 예측/GT 부피가 1.28~1.55 로 부풀고 Dice=0 제외 구간 Dice 가 0.556~0.612 로 하락 = **과대분할만 유발**. (5) **순이득 개입 = `slice_weight: inv_volume`, `slice_weight_power: 0.5`** (환자 병변 부피의 −0.5 제곱으로 양성 슬라이스 복원추출, step 수·LR 스케줄 불변). **5시드 [측정]: <2 mL Dice 0.5430 → 0.5733 (+0.0302 = 시드 SD 의 2.19배, 짝지은 환자 부트스트랩 CI [+0.006, +0.061]) · 전체 val Dice 0.6822 → 0.6929 (5시드 전부 기준선 최대값 초과) · 부피 ICC 0.8782 → 0.9134 · 민감도 0.966 → 0.971 · [2,10) +0.013 / [10,50) −0.002 / [50,∞) +0.002**. 메커니즘: 하드 플로어가 **7명 → 4명**으로 줄어든 것(= 못 찾던 3명을 찾음)이 전부이고, 찾아낸 환자의 Dice 는 그대로. 지수를 1.0 으로 올리면 소병변은 +0.037 로 더 오르지만 ICC 가 0.834 로 무너져 **0.5 가 유일하게 전 지표가 함께 좋아지는 지점**(용량-반응 단조 교환관계 측정) | `dataset.py` 에 `slice_weight`/`slice_weight_power`(H2)·`stack_context()`+`context`/`context_mode`(H4), `losses.py` 에 `tversky_loss`·`sample_invsize` reduction(H5), `train.py`/`eval_seg.py`/`predict_masks.py` 배선 — **전부 기본값이 기존 동작과 동일**. 검증: 수정 후 코드로 40 epoch 재학습해 **40 epoch 전부 부동소수 일치·`best.pt` sha256 `e10d9268…` 바이트 동일**. `configs/seg_unet2d.yaml` 활성값 **무변경**(새 키는 주석으로만), 권장 설정은 `configs/seg_unet2d_invvol.yaml` 로 분리. 회귀 테스트 +17 → **pytest 52 → 69 통과**. 분석 스크립트 `scripts/analysis/small_lesion_{stats,failure_modes,experiments,report,threshold}.py`, 산출물 `results/small_lesion/`, run 37개(`runs/small_*`, 8 variant × 3~5 시드 + 기준선 시드 2개 추가). **test 미열람**: 새 모델은 하나도 test 에 올리지 않았고 지표도 계산하지 않았다 — 헌장 §C(재학습·재선택 금지)에 따라 이 결과는 **다음 학습 사이클의 근거**로만 남긴다 |
